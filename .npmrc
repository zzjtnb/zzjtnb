# 幽灵依赖问题
# 提升所有依赖到根 node_modules 目录下,相当于 public-hoist-pattern[]=*
shamefully-hoist=true
# 如果启用了此选项,那么在依赖树中存在缺失或无效的 peer 依赖关系时,命令将执行失败。
strict-peer-dependencies=false
# 禁用 package-lock.json 文件
package-lock = false
# 允许安装依赖时使用旧版本的 peerDependencies 版本
legacy-peer-deps=true